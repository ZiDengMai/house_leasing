spring:
  application:
    name: house_leasing
  datasource:
    url: jdbc:mysql://localhost:3306/house_leasing?characterEncoding=UTF-8&AutoReconnect=true
    username: root
    password: 0096unicorn
    driver-class-name: com.mysql.jdbc.Driver

mybatis:
  mapper-locations: classpath:/mapper/**/*.xml

server:
  port: 8080